#include<stdio.h>

void main()
{
	int num , factorial=1,i=1;
	
	printf("Enter The number : ");
	scanf("%d",&num);
	
	while(i<num)
	{
	 factorial*=num;
	 num--;
	}
	printf("The factorial is %d",factorial);
}
